  <div class="container mx-auto py-20">
    <h1 class="font-bold text-white text-3xl mb-8 text-center">Draft Blog Posts</h1>
    <% if @blog_posts.empty? %>
      <h2 class="font-bold text-3xl mb-4 text-center">Yeah!! You don't have any drafts..</h1>
    <% else %>
      <%= render 'blogpost' %>
    <% end %>
  </div>

